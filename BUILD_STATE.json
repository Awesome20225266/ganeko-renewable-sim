{
  "project": "Renewable Generation Simulation Platform",
  "build_complete": true,
  "current_phase": 10,
  "last_completed_step": "Fixed NIGHT-SOLAR bug + set real site. Root cause: corrupted/partial weather feeds (e.g. historical mode on future/partial dates) could leave nonzero irradiance at night which bypassed the data-based night check. Fix: app/solar_geom.py (NOAA sun-elevation) + normalize_to_blocks now takes lat/lon/timezone and forces poa=0 & is_day=0 whenever sun is below horizon (block midpoint, -0.833 deg); radiation NaN->0 (no daytime back-fill). simulate passes spec coords. Default site -> Ganeko Two Energy Private Limited (17.60N, 76.36E, Asia/Kolkata) in settings + .env; running plant updated to v6. 40 tests pass (2 new gate tests); verified night solar=0 locally + via public tunnel; proper midday bell, Solar CUF 23.9%.",
  "next_action": "DONE. Permanent host: user pushes to GitHub + Render Blueprint. Live tunnel: https://compute-retired-rewards-mrs.trycloudflare.com (while PC+cloudflared run).",
  "design_decisions": [
    "Site location not present in Book1 (4).xlsx (verified: no lat/long text/coordinates anywhere in the 35k-row sheet). Per spec, location is a configurable placeholder. Seed defaults to Jaisalmer, Rajasthan (26.9124, 70.9026, Asia/Kolkata) - strong solar+wind. Override via .env (PLANT_LAT/PLANT_LON/PLANT_TZ) or DB.",
    "DB: SQLAlchemy via DATABASE_URL; SQLite zero-config local default, Postgres in docker-compose. Alembic migration (187b7cae8814) + create_all bootstrap fallback.",
    "Open-Meteo: wind requested in m/s (wind_speed_unit=ms) for the power curve. minutely_15 used natively when present, else robust hourly->15min interpolation; ERA5 archive (hourly) flagged interpolated.",
    "Single generation_block table holds the per-block hybrid record (solar+wind+total), queryable by data_mode; reprocess preserves history via is_current demotion + versioned rows. Documented as a justified deviation from 3 separate output tables.",
    "Scheduler: APScheduler in-process (daily + live-refresh) and standalone runner for the docker scheduler service. Dashboard served by FastAPI at /dashboard with open read-only feeds (no secrets).",
    "Capacities from workbook: Solar AC=160, DC=240 (1.5), Wind AC=135, PPA=140, 96 15-min blocks/day."
  ],
  "tasks": [
    {"id": 1, "name": "Scaffold, config, DB models, migrations, seed", "status": "done"},
    {"id": 2, "name": "Open-Meteo client + raw storage + normalization", "status": "done"},
    {"id": 3, "name": "Solar/wind/hybrid engines + persistence + versioning", "status": "done"},
    {"id": 4, "name": "FastAPI endpoints + auth + rate limit + logging", "status": "done"},
    {"id": 5, "name": "Scheduler + quality + dashboard + reprocess + key mgmt", "status": "done"},
    {"id": 6, "name": "Tests + Docker + README + e2e + health report + BUILD_COMPLETE", "status": "done"}
  ]
}
